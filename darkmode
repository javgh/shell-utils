#!/usr/bin/env bash

xfconf-query -c xsettings -p /Net/ThemeName -s "Adwaita-dark"
if [[ -L "$HOME/.config/alacritty/alacritty.toml" ]]; then
    cd $HOME/.config/alacritty/
    rm alacritty.toml
    ln -s alacritty_dark.toml alacritty.toml
fi
